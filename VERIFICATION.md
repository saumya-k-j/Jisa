# VERIFICATION (living)

Status one of: verified / unverified / needs-live-validation.
All commands run from the repo root. Sanitizer trees are configured with
`-DCMAKE_CXX_COMPILER=/opt/homebrew/opt/llvm/bin/clang++` (see DECISIONS D-007).

| Capability | Command to reproduce | Status |
|---|---|---|
| Phase-1 unit tests pass (43/43): Message layout, SPSC/MPSC ring buffer incl. 1M-msg 2-thread and 4-producer stress, pool allocator | `cmake -B build -G Ninja && cmake --build build && ctest --test-dir build --output-on-failure` | verified (2026-07-07) |
| ASan + UBSan clean, 43/43 | `cmake -B build-asan -G Ninja -DCMAKE_BUILD_TYPE=Debug -DSANITIZE=address,undefined -DCMAKE_CXX_COMPILER=/opt/homebrew/opt/llvm/bin/clang++ && cmake --build build-asan && ctest --test-dir build-asan --output-on-failure` | verified (2026-07-07) |
| TSan clean, 43/43 (lock-free ring buffers under concurrency) | `cmake -B build-tsan -G Ninja -DCMAKE_BUILD_TYPE=Debug -DSANITIZE=thread -DCMAKE_CXX_COMPILER=/opt/homebrew/opt/llvm/bin/clang++ && cmake --build build-tsan && ctest --test-dir build-tsan --output-on-failure` | verified (2026-07-07) |
| Compile clean under -Wall -Wextra -Werror (project targets; third-party via -isystem, D-009) | any build command above (flags via telemetry_warnings target) | verified (2026-07-07) |
| SPSC two-thread throughput ~34M msgs/sec (Apple Silicon, Release) | `./build/benchmarks/bench_core` | verified (2026-07-07) |
| Latency percentiles: spsc push/pop and pool alloc/dealloc p50≈41ns p99≈42ns (timer-floor upper bound, D-008); mpsc 4-producer push p50≈583ns p99≈3.8µs p99.9≈8.1µs | `./build/benchmarks/bench_core` | verified (2026-07-07) |
| Phase-2 feed tests pass (26 new; 69/69 total): Coinbase parse (ticker/heartbeat/malformed/unmapped), trade_id gap detection incl. heartbeat last_trade_id, reconnect state reset, end-to-end fixture→ring buffer | `cmake -B build -G Ninja && cmake --build build && ctest --test-dir build --output-on-failure` | verified (2026-07-07) |
| Phase-2 ASan+UBSan clean 69/69; TSan clean 69/69 | same sanitizer-tree commands as phase-1 rows above | verified (2026-07-07) |
| Live Coinbase WSS ingestion end-to-end (real endpoint): 20s run, 198 msgs received, 157 ticks pushed (BTC 86 / ETH 71), 0 parse failures, 0 gaps | `cmake -B build -G Ninja -DBUILD_LIVE_SMOKE=ON && cmake --build build && ./build/src/feed/coinbase_live_smoke --seconds 20` | verified (2026-07-07, this machine/network; long-soak + reconnect-under-real-outage behavior needs-live-validation) |
| Gap detection fires on real dropped messages from the live feed (not just synthetic) | needs a real feed drop; synthetic gaps covered in tests/feed/test_gap_detection.cpp | needs-live-validation |
| Phase-3 replay tests pass (33 new; 102/102 total): TREC round-trip (synthetic + real Coinbase fixture via adapter), FNV-1a known-answer checksum, double-replay identical digest, truncated/garbage/bad-magic handling, all four fault types with exact label boundaries, seeded injection determinism | `cmake --build build && ctest --test-dir build --output-on-failure` | verified (2026-07-08) |
| Phase-3 ASan+UBSan clean 102/102; TSan clean 102/102 (incl. after minor-fix pass) | sanitizer-tree commands above | verified (2026-07-08) |
| Replay determinism (SPEC objective #5): identical checksummed end-state on replay of a recorded stream | `ctest --test-dir build -R ReplayDeterminism --output-on-failure` | verified (2026-07-08) |
| Phase-4 detect tests pass (31 new; 133/133 total): rules bounds/rate/YAML-loader errors, EWMA known-answer + zero-variance/NaN guards, CUSUM known-answer + reset-after-fire + false-alarm sanity, end-to-end detection delay via injected drift | `cmake --build build && ctest --test-dir build --output-on-failure` | verified (2026-07-08) |
| Phase-4 ASan+UBSan clean 133/133; TSan clean 133/133 | sanitizer-tree commands above | verified (2026-07-08) |
| Detection delay measured (objective #3): CUSUM fires 12 samples after injected drift onset (seed 12345, drift magnitude 5.0 over 300 samples, alpha=0.01 k=3 h=8), zero pre-onset false alarms | `ctest --test-dir build -R DetectionDelay --output-on-failure` (delay printed in test output) | verified (2026-07-08) |
| BOCPD research prototype: 4/4 pytest; seeded comparison vs reference CUSUM (step: both delay 0; drift: BOCPD 34 vs CUSUM 28 samples) | `python/.venv/bin/python -m pytest python/research/test_bocpd.py -q && python/.venv/bin/python python/research/compare_cusum.py` | verified (2026-07-08) |
| Phase-5 conformal tests pass (12 new; 145/145 total): quantile known answers, eviction/adaptivity, per-stream independence, pre-update decision semantics, alpha edge cases | `cmake --build build && ctest --test-dir build --output-on-failure` | verified (2026-07-08) |
| Phase-5 ASan+UBSan clean 145/145; TSan clean 145/145 | sanitizer-tree commands above | verified (2026-07-08) |
| False-alarm budget held empirically (objective #4): stationary stream (seed 0xC0FFEE1234, N=50k, W=500) → empirical rate 0.01158 at alpha=0.01 and 0.05184 at alpha=0.05, inside pre-derived binomial 8-sigma bands, above alpha/3 vacuity floor; bands derived before implementation existed (Python reference) | `ctest --test-dir build -R ConformalFalseAlarmRate --output-on-failure` | verified (2026-07-08) |
| Phase-6 adapter tests pass (31 new: 12 grid + 19 ADS-B; 177/177 total): batch parse, dedup across overlapping polls, wall-clock gap detection (grid), hex→stream registry with cap, "ground" handling, per-aircraft dedup/seq (ADS-B), malformed-input rejection both | `cmake --build build && ctest --test-dir build --output-on-failure` | verified (2026-07-08) |
| Phase-6 ASan+UBSan clean 177/177; TSan clean 177/177 | sanitizer-tree commands above | verified (2026-07-08) |
| ADS-B adapter against REAL recorded feed data (60 adsb.fi polls, 26 aircraft, Heathrow area) | `ctest --test-dir build -R Adsb` (fixture provenance: tests/feed/fixtures/README.md) | verified (2026-07-08; live-poll ingestion loop itself not exercised — REST polling is trivial curl, adapter parse is what matters) |
| Grid adapter live ingestion (Gridradar/Fingrid) — requires registered API key not available in this environment; adapter verified against SYNTHETIC format-faithful fixture only | would need: user-provided Gridradar or Fingrid key + a polling smoke run | needs-live-validation |
| Phase-7 bindings + API: 28/28 python tests (13+2 binding tests incl. record/replay checksum round-trip, fault-label format, EWMA/conformal/CUSUM known answers matching C++ derivations; 9 API tests incl. end-to-end .trec→pipeline→alert→SQLite, persistence across app recreation; 4 bocpd) with C++ suite unaffected at 177/177 | `python/.venv/bin/python -m pytest python/ -q` (bindings built: `cmake -B build -G Ninja -DBUILD_PYTHON_BINDINGS=ON -DPython3_EXECUTABLE=$PWD/python/.venv/bin/python && cmake --build build --target telemetry_engine && cp build/src/bindings/telemetry_engine.cpython-314-darwin.so python/.venv/lib/python3.14/site-packages/`) | verified (2026-07-08) |
| /status latency histograms (SPEC 3.11) | NOT implemented — omitted, not faked (D-040); hot path doesn't export per-message latency yet | unverified |
| Phase-8 agent tests pass (41 new incl. 8 coverage additions; 69/69 python total): tools closed-interval/no-future-leak semantics, JSON tool-loop protocol (retry-once, max_turns, hypothesis validation, bad-kwargs feedback), evaluation math + label matching — all deterministic mock-LLM, zero live calls in pytest | `python/.venv/bin/python -m pytest python/ -q` | verified (2026-07-08) |
| REAL LLM triage evaluation vs injected ground truth (SPEC 3.12): seed 42, 10 alerts, grid domain — haiku/6-turns: accuracy 0.400, judge 0.295; sonnet/6-turns: 0.000 (all max-turns — turn budget dominates); sonnet/12-turns: 0.400, judge 0.290, all memos substantive. Full auditable tables: docs/eval_runs/ | `PATH="/usr/local/bin:$PATH" python/.venv/bin/python python/agent/run_eval.py --seed 42 --model haiku --max-cases 10 --max-turns 6` (LLM side nondeterministic; stream/fault side seeded) | verified (2026-07-08) |
| Zero heap allocation on hot path after warmup — TEST-VERIFIED via global operator new/delete counting: 0 steady-state allocations in (a) Coinbase parse+SPSC ring over 450 real fixture lines, (b) all four detect layers over 10k samples × 2 streams, (c) 100k pool allocate/deallocate cycles (warmup allocations per D-006/D-011/D-012/D-025/D-026 excluded by design) | `ctest --test-dir build -R HotPathAllocation --output-on-failure` | verified (2026-07-08) |
| Sanitizers run in CI | .github/workflows/ci.yml authored (4 jobs: Release+ctest incl. explicit replay-determinism step, ASan+UBSan with .ubsan-ignorelist for simdjson's pre-existing __builtin_clz(0), TSan, python bindings+pytest); YAML validated locally; CANNOT run Actions from this environment — unverified until pushed to GitHub | needs-live-validation |
| Final totals: 180/180 C++ tests (Release, ASan+UBSan, TSan) + 69/69 python tests | commands above | verified (2026-07-08) |

Notes:
- Uncontended p50/p99 latencies at ~41 ns are dominated by steady_clock timer
  overhead; treat them as upper bounds (stated in bench output and D-008).
- Benchmark numbers are from this dev machine (Apple Silicon, macOS); numbers
  on target hardware are needs-live-validation.
